if theme.comment.duoshuo
  .ds-thread(data-thread-key=page.path, data-title=page.title, data-url=page.permalink, data-author-key='1')

if theme.comment.disqus
  #disqus_thread

if theme.comment.livere
  #lv-container(data-id="city",data-uid=theme.comment.livere)

if theme.comment.uyan
  #uyan_frame

if theme.comment.cloudTie
  #cloud-tie-wrapper.cloud-tie-wrapper

if theme.comment.changyan.appid && theme.comment.changyan.appkey
  #SOHUCS(sid=page.path)

if theme.gitalk.enable
   #container
    if theme.cdn.enable
      script(type='text/javascript' src=theme.cdn.path + '/static/js/gitalk.min.js')
      script(type='text/javascript' src=theme.cdn.path + '/static/js/md5.js')
    else
      script(type='text/javascript' src='https://unpkg.com/gitalk/dist/gitalk.min.js')
      script(type='text/javascript' src='https://cdn.bootcss.com/blueimp-md5/2.10.0/js/md5.js')
    script.
      var gitalk = new Gitalk({
        clientID: '#{theme.gitalk.client_id}',
        clientSecret: '#{theme.gitalk.client_secret}',
        repo: '#{theme.gitalk.repo}',
        owner: '#{theme.gitalk.owner}',
        admin: ['#{theme.gitalk.admin}'],
        id: md5(window.location.pathname),
        distractionFreeMode: false,
        language: '#{theme.gitalk.language}',
        pagerDirection: '#{theme.gitalk.pagerDirection}',
        proxy: '#{theme.gitalk.proxy}'
      })
      gitalk.render('container')

      
